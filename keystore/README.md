# Signing key backup

Restore to `~/.android/debug.keystore` on a new machine before building
updates — installed apps only accept updates signed with this key.
Store password: android (Android default debug keystore).
