# Firebase / Credential Manager libraries ship their own consumer rules.
# We only read Firestore data via explicit maps (no reflection models),
# so no extra keeps are required. Add rules here if a library breaks.
